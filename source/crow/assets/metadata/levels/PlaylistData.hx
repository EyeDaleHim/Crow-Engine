package crow.assets.metadata.levels;

import crow.assets.metadata.levels.LevelData.LevelInfoData;

typedef PlaylistData = Array<PlaylistOptionData>;

typedef PlaylistOptionData = {
    > LevelInfoData,
};